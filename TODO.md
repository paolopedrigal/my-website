## TODO:
- Add the fade in effect on the lines for the timeline
- Change size of nav bar when hovering mouse over it?
- Provide link to resume
- Fix link to email
- Fix warnings when compiling
- Update resume
- Put components in their own folders
- Add json file for js files to read from instead of hardcoding information
- Fix quote consistency

## Notes:
- Use `npm run deploy` when making changes to published website in main branch. (The gh-pages branch will automatically be pushed because of npm gh-pages?)